const fs =require('fs')

const directorypath='./public'

fs.readdir(directorypath,(err,files)=>{
    if(err)return console.log(err)
    console.log(files);
b
    fs.readFile(
        filename,function rv(err,files)
    )

})